# Production example env (copy to your deployment provider)
NODE_ENV=production

# Public (client)
NEXT_PUBLIC_APP_NAME=Schoolverse_1
NEXT_PUBLIC_APP_URL=https://schoolverse-1.vercel.app

# Server / Database
DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<db>?schema=public

# Auth
JWT_SECRET=please_replace_with_strong_secret
JWT_EXPIRES_IN=1d

# Optional services
REDIS_URL=redis://<managed-redis-host>:6379
